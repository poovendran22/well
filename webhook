pipeline {
    agent any
    stages {
        stage("Env Variables") {
            steps {
                sh "printenv"

	 stage("Job2") {
                    steps {
                        echo "hi poovendran"

		   }
		}
            }
        }
    }
}
